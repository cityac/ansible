- hosts: localhost
  become: true
  pre_tasks:
    - name: Update the apt cache
      apt:
        update_cache: yes
      tags: 
        - node
        - zsh
  tasks:
    - import_tasks:  fem_tasks/zsh.yml
    - import_tasks:  fem_tasks/node.yml
    
    

